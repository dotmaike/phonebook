import TopBar from './../TopBar';
import Container from './../Container/Container';

const App = () => (
    <div>
        <TopBar/>
        <br/>
        <Container/>
    </div>
);

export default App;
